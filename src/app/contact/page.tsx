import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return (
    <main className="h-full flex flex-col min-h-screen ">

      
    </main>
  )
}

export default page